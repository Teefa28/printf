#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <string.h>
#include <stdarg.h>
#include <unistd.h>
#include <stdlib.h>

int teefa_ptchr(char c);
int main(void);
int _printf(const char *format, ...);
int printf(const char *format, ...);

#endif            
