#ifndef MAIN_H
#define MAIN_H

#include <limits.h>
#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <stdarg>

int main(void);
int _printf(const char *format, ...);
int printf(const char *format, ...);
#endif            
